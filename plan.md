Create a PuAiA plugin that does the following every day at 6 AM:

- uses firebase to retrieve data from https://www.capeet.com/gigs_list.html
- implements everything that it can be stored in the vector store
